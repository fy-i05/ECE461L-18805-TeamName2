{
  "name": "ece461l-root",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "heroku-postbuild": "cd ee461l-portal && npm install --include=dev && npm run build",
    "start": "node ee461l-portal/server.js"
  },
  "engines": {
    "node": "22.x"
  }
}
