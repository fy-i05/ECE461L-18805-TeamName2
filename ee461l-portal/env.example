# MongoDB Atlas Connection String
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<database>?retryWrites=true&w=majority
# Replace <username>, <password>, <cluster>, and <database> with your actual values
MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/ee461l_portal?retryWrites=true&w=majority

# Session secret for express-session (use a strong random string in production)
SESSION_SECRET=change-me-dev

